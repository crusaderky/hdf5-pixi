[workspace]
channels = ["conda-forge"]
name = "hdf5"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
cmake = {cwd = "hdf5", cmd = "cmake --workflow --preset=ci-StdShar-GNUC --fresh"}

[dependencies]
cmake = "*"
compilers = "*"
ninja = "*"
openjdk = "*"
